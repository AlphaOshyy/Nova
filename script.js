const cursor=document.querySelector('.cursor'),label=document.querySelector('.cursor-label'),progress=document.querySelector('.progress');
const reveals=document.querySelectorAll('.reveal');
window.addEventListener('mousemove',e=>{if(cursor){cursor.style.left=e.clientX+'px';cursor.style.top=e.clientY+'px'}if(label){label.style.left=e.clientX+'px';label.style.top=e.clientY+'px'}});
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12});reveals.forEach(e=>observer.observe(e));
const update=()=>{const max=document.documentElement.scrollHeight-innerHeight;if(progress)progress.style.width=(max>0?scrollY/max*100:0)+'%'};window.addEventListener('scroll',update,{passive:true});update();
const hoverTargets=document.querySelectorAll('a,.case,.lab-grid>div,.lab-image');hoverTargets.forEach(el=>{el.addEventListener('mouseenter',()=>{cursor?.classList.add('active');if(label)label.style.opacity='1';if(label)label.textContent=el.classList.contains('case')?'VIEW PROJECT':'EXPLORE'});el.addEventListener('mouseleave',()=>{cursor?.classList.remove('active');if(label)label.style.opacity='0'})});
document.querySelectorAll('.magnetic').forEach(el=>{el.addEventListener('mousemove',e=>{const r=el.getBoundingClientRect(),x=(e.clientX-r.left-r.width/2)*.12,y=(e.clientY-r.top-r.height/2)*.12;el.style.transform=`translate(${x}px,${y}px)`});el.addEventListener('mouseleave',()=>el.style.transform='')});
document.querySelectorAll('.case').forEach(card=>{card.addEventListener('mousemove',e=>{if(innerWidth<801)return;const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(1400px) rotateX(${y*-1}deg) rotateY(${x*1}deg)`});card.addEventListener('mouseleave',()=>card.style.transform='')});
const hero=document.querySelector('.hero');window.addEventListener('scroll',()=>{if(!hero||innerWidth<801)return;const y=Math.min(scrollY,innerHeight),center=hero.querySelector('.hero-center'),orb=hero.querySelector('.hero-orb'),image=hero.querySelector('.hero-image');if(center)center.style.transform=`translateY(${y*.1}px)`;if(orb)orb.style.transform=`translate3d(${y*.02}px,${y*.04}px,0)`;if(image)image.style.transform=`translateY(${y*.06}px)`},{passive:true});
